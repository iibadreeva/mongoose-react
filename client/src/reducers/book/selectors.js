export const booksSelector = (state) => state.book.list;
export const bookSelector = (state) => state.book.book;
export const loadSelector = (state) => state.book.load;
export const errorSelector = (state) => state.book.error;
